function esPar(numero) {
    if (numero % 2 === 0) {
        console.log(`${numero} es par`);
        return true;
      } else {
        console.log(`${numero} no es par`);
        return false;
      }
    }

esPar(110)